@model Entities.DTOs.DoctorLoginDto
@{
    ViewData["Title"] = "Doktor Girişi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white text-center">
                    <h3>Doktor Girişi</h3>
                </div>
                <div class="card-body">
                    @if (ViewBag.Error != null)
                    {
                        <div class="alert alert-danger">@ViewBag.Error</div>
                    }

                    <form asp-action="Login" method="post">
                        <div class="mb-3">
                            <label asp-for="DoctorEmail" class="form-label">Email</label>
                            <input asp-for="DoctorEmail" class="form-control" type="email" required />
                            <span asp-validation-for="DoctorEmail" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="DoctorPassword" class="form-label">Şifre</label>
                            <div class="input-group">
                                <input asp-for="DoctorPassword" id="doctorLoginPassword" class="form-control" type="password" required />
                                <button class="btn btn-outline-secondary" type="button" id="toggleDoctorLoginPassword">
                                    <i class="fas fa-eye" id="toggleDoctorLoginIcon"></i>
                                </button>
                            </div>
                            <span asp-validation-for="DoctorPassword" class="text-danger"></span>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-info">Giriş Yap</button>
                        </div>
                    </form>

                    <div class="text-center mt-3">
                        <p>Hesabınız yok mu? <a asp-action="Register" class="text-decoration-none">Kayıt Ol</a></p>
                        <p><a asp-controller="Home" asp-action="Index" class="text-decoration-none">Ana Sayfaya Dön</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // Şifre göster/gizle
    document.addEventListener('DOMContentLoaded', function() {
        const togglePassword = document.getElementById('toggleDoctorLoginPassword');
        const passwordInput = document.getElementById('doctorLoginPassword');
        const toggleIcon = document.getElementById('toggleDoctorLoginIcon');
        
        if (togglePassword && passwordInput && toggleIcon) {
            togglePassword.addEventListener('click', function() {
                if (passwordInput.type === 'password') {
                    passwordInput.type = 'text';
                    toggleIcon.classList.remove('fa-eye');
                    toggleIcon.classList.add('fa-eye-slash');
                } else {
                    passwordInput.type = 'password';
                    toggleIcon.classList.remove('fa-eye-slash');
                    toggleIcon.classList.add('fa-eye');
                }
            });
        }
    });
</script>

